spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/Desktop/WebApp_springJPA/H2/bin/jpashop2
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #        show_sql: true # System out ?? ??
        format_sql: true
#        default_batch_fetch_size: 100
#    open-in-view: false


  jackson:
    serialization:
      fail-on-empty-beans: false


logging.level:
  org.hibernate.SQL: debug  # ????? ??
  org.hibernate.type: trace